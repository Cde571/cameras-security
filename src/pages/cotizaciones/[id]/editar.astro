---
// src/pages/cotizaciones/[id]/versionar.astro
export const prerender = false;

import MainLayout from "../../../layouts/MainLayout.astro";
import VersionControl from "../../../components/cotizaciones/VersionControl";

const { id } = Astro.params;
---

<MainLayout title={`Versionar cotización ${id}`}>
  <VersionControl client:load cotizacionId={id} />
</MainLayout>

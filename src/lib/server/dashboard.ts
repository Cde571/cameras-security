/**
 * Archivo mantenido por compatibilidad.
 * La fuente principal del dashboard ahora vive en:
 * - /api/reportes/dashboard
 * - src/lib/services/dashboardService.ts
 */
export async function getDashboardData() {
  return null;
}
